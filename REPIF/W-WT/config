<?php

require("db.php");
if(!isset($_GET["HostName"])) {
    echo "No HostName specified";
    exit;
}

$fileText = array(

'groups' => array('group1' => array('scripts' => ["script1.sh", "script2.sh"], 'leds' => [[16, "BLINK", 5], [18, "TOGGLE", -1]])),
'inputs' => [["Garage Light", ["group1"], 10]]

);

//Convert the array into YAML content

$data = yaml_emit($fileText);

//Print the array

echo "<pre>".$data."</pre>";